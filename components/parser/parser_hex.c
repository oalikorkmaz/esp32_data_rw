#include "parser_hex.h"
#include "esp_log.h"

static const char *TAG = "PARSER_HEX";

esp_err_t parser_init(void) {
    ESP_LOGI(TAG, "PARSER bileşeni başlatılıyor (HEX Protokolü).");
    // Burada Parser için gerekli başlangıç ayarları (varsa) olacak.
    
    return ESP_OK;
}